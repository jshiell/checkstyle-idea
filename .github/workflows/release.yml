name: Release

on:
  push: 
    tags: 
    - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Java 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    
    - name: Build
      run: ./gradlew check jacocoCsaccessCoverageVerification buildPlugin

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        CHANGELOG: $(cat ./CHANGELOG.md | grep 5.45.0 | sed 's%^\*[[:space:]]*\*\*[0-9.]*\*\*[[:space:]]*%%g')
      with:
        tag_name: ${{ github.ref }}
        release_name: ${{ github.ref }}
        body: $CHANGELOG
        draft: false
        prerelease: false

    - name: Upload Release Asset
      id: upload-release-asset 
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./build/distributions/CheckStyle-IDEA-${{ github.event.release.tag_name }}.zip
        asset_name: CheckStyle-IDEA-${{ github.event.release.tag_name }}.zip
        asset_content_type: application/zip